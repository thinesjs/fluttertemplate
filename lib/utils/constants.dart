import 'dart:ui';

class tPadding {
  tPadding._();

  static const double tDefaultPadding = 20.0;
  static const double tVerticalPadding = 40.0;
  static const double tRoundEdgePadding = 20.0;
}
class tColors {
  tColors._();
  static const Color backgroundDark = Color(0xff181b23);
  static const Color cardColorDark = Color(0xff282932);
  static const Color cardColorLight = Color(0xffe5e5f1);

  static const Color cardColorDarkLoading = Color(0xff353542);
  static const Color cardColorLightLoading = Color(0xffb0b0b7);
}

class tSizes {
  tSizes._();

  static const double titleSize = 30;
}